# LOSOMA — SEO и индексация, актуальный срез

Дата: 2026-07-30

Production: `https://losoma.de`

Search Console account: `maxim@losoma.de`

## Краткий вывод

- Технический production исправен: все 15 canonical URL отвечают `200`, побайтно совпадают с
  проверенным `dist/`, индексируемы и присутствуют в sitemap.
- Канонизация старых/дублирующих URL завершена: HTTP, `www`, `.html`, trailing slash, старые
  `/privacy*` и blog aliases дают постоянные `301` на единственный canonical URL.
- Sitemap повторно отправлен 2026-07-30; Google обнаружил 15 URL.
- Главный остаточный SEO-риск не технический: новым service pages нужны обход Google и более
  самостоятельная ценность, реальные кейсы, фото и внешние сигналы доверия.
- Hero/performance release опубликован. Production mobile lab trace: LCP `2,978 ms`, CLS `0`;
  Lighthouse Accessibility/Best Practices/SEO/Agentic Browsing `100/100/100/100`.

## Production и canonical QA

- Все URL из `sitemap.xml` возвращают `200` без redirect chain.
- `robots.txt` и `sitemap.xml` возвращают `200`.
- Все production HTML responses совпадают по SHA-256 с соответствующими файлами `dist/`.
- `canonical`, `robots index, follow`, Open Graph и JSON-LD прошли локальный strict SEO audit.
- Проверены `301`:
  - `http://losoma.de/` -> `https://losoma.de/`;
  - `https://www.losoma.de/` -> `https://losoma.de/`;
  - `/index.html` -> `/`;
  - service `.html` и trailing-slash -> clean URL;
  - `/blog/`, `/blog/index.html` -> `/blog`;
  - article `.html`/trailing-slash -> clean article URL;
  - `/privacy`, `/privacy/`, `/privacy.html` -> `/datenschutz`;
  - `/impressum/`, `/impressum.html` -> `/impressum`.
- Query string сохраняется; redirect loops не обнаружены.

## Search Console — индексация

Sitemap:

- повторно отправлен 2026-07-30;
- отправка успешна;
- обнаружено 15 URL.

Агрегированный отчёт `Seiten` на момент проверки всё ещё использовал срез 2026-07-24:

- 6 URL индексировано;
- 13 URL не индексировано;
- два host/scheme redirect URL являются нормальными исключениями и не требуют исправления.

Актуальный URL Inspection имеет приоритет над задерживающимся агрегированным отчётом.

Подтверждённо индексируются:

- `/blog`;
- `/gewerbliche-reinigung`;
- `/industriereinigung`;
- `/fassaden-hoehenarbeiten`.

Подтверждённые запросы индексации:

- `/hausmeisterservice`;
- `/grundreinigung`.

Их не отправлять повторно без новой содержательной причины.

Ожидающие URL:

- `/garten-landschaftspflege` — Live URL Test успешен, запрос завершился общей ошибкой Google;
- `/solaranlagenreinigung` — Live URL Test успешен, запрос завершился общей ошибкой Google;
- `/treppenhausreinigung` — запрос завершился той же общей ошибкой;
- `/kontakt` — `URL ist Google nicht bekannt`;
- `/impressum` — `Gefunden – zurzeit nicht indexiert`.

Следующий агент проверяет только эти пять canonical URL. Для каждого: URL Inspection -> при
необходимости Live URL Test -> запрос только после успешного live result. Успех фиксируется лишь
при явном `Indexierung wurde beantragt`. Не отправлять redirects, `.html` или trailing-slash дубли.

## Search Console — поисковая эффективность

Последний доступный быстрый срез `Leistung`:

- 5 кликов;
- 11 показов;
- CTR `45,5%`;
- средняя позиция `1,6`;
- запрос `losoma`: 4 клика / 5 показов.

Полевых данных по небредовым коммерческим запросам пока недостаточно. Ручной поиск Google не
считать точным rank tracker: он зависит от геолокации, персонализации, устройства и истории.

Следующий сопоставимый отчёт снимать через 7–14 дней после release 2026-07-30, используя одинаковые
28-дневные периоды, страну Deutschland и раздельные Mobile/Desktop срезы.

## Performance и UX

- First-visit loader не ждёт видео; MP4 запускается после `window.load` и idle.
- Save-Data/2g/reduced-motion сохраняют poster.
- Poster: `133,868` байт.
- Hero MP4 сохранён без дополнительного ухудшения качества: `1920x1080`, `5,731,171` байт.
- Три главные карточки используют responsive AVIF/WebP `480/768/1200`.
- Production trace `Slow 4G`, CPU `4x`, `412x915@3`: LCP `2,978 ms`, CLS `0`, TTFB `511 ms`;
  LCP — текстовый `H1#hero-title`.
- Production screenshots mobile/desktop подтвердили читаемый H1 поверх видео.
- CrUX page-level data пока отсутствуют; lab trace не заменяет Core Web Vitals field report.

## Приоритеты

### P0 — внешние блокеры

1. Завершить передачу существующего Google Business Profile на `maxim@losoma.de` через support
   case `2-2514000041594`; дубликат не создавать.
2. Через 7–14 дней повторно проверить пять ожидающих canonical URL и Search Console performance.

### P1 — content quality

1. Уточнить Title/H1 главной под основной intent `Gebäudeservice Berlin` без переспама.
2. Усилить `/grundreinigung` и `/hausmeisterservice` реальными границами работ, частотой,
   оборудованием, безопасностью, факторами цены, кейсами, фото и отдельными FAQ.
3. Затем усилить `/treppenhausreinigung`, `/garten-landschaftspflege` и
   `/solaranlagenreinigung` без шаблонного синонимического рерайта.
4. Добавить контекстные внутренние ссылки из релевантных материалов и реальные внешние упоминания.

### P1 — trust/local

1. После GBP transfer подтвердить часы, категории, service area и Profile/Maps URL.
2. Добавлять только реальные фото и отзывы; не покупать и не стимулировать фиктивные отзывы.
3. Не добавлять в Schema неподтверждённые `geo`, `openingHours`, `foundingDate` или `priceRange`.

## Источники продолжения

- Технические и контентные задачи: `SEO_CHECKLIST.md`.
- Мониторинг запросов: `SEO_RANKING_CHECKLIST.md`.
- Точка возобновления: `HANDOFF.md`.
- Google ownership: `GOOGLE_ACCOUNT_TRANSFER_CHECKLIST.md`.
