{
  "name": "losoma",
  "version": "0.1.0",
  "private": true,
  "description": "Static LOSOMA website for Hostinger production.",
  "scripts": {
    "build:static": "node scripts/build-static.mjs",
    "build": "npm run build:static",
    "audit:classes": "node scripts/audit-client-first-classes.mjs",
    "audit:classes:strict": "node scripts/audit-client-first-classes.mjs --strict",
    "audit:seo": "node scripts/audit-seo.mjs",
    "deploy": "npm run build && node -e \"console.error('Production deploy is Hostinger-only. Follow HOSTINGER_LAUNCH_CHECKLIST.md.'); process.exit(1)\""
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
